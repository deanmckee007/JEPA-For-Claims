[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jepa-for-claims"
version = "0.0.1"
dependencies = ["lightning"]

[tool.setuptools.packages.find]
where = ["."]
