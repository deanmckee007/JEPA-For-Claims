# Invalid experiment

This run used a fully class-balanced add/remove event loss. That objective
shifted rare-event probabilities toward 0.5, so add, remove, and retain scores
were not mutually calibrated. Do not use these metrics.
